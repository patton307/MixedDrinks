var Backbone = require('backbone');
var $ = require('jquery');
var _ = require('underscore');

module.exports = Backbone.Model.extend({
  // urlRoot: '/users',
  // idAttribute: '_id',
  // initialize: function() {
  //
  // }
});
